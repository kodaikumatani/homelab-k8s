apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: monitoring
  namespace: monitoring
spec:
  interval: 5m
  chart:
    spec:
      chart: kube-prometheus-stack
      version: "82.x"
      sourceRef:
        kind: HelmRepository
        name: prometheus-community
      interval: 1h
  values:
    grafana:
      admin:
        existingSecret: grafana-admin
        userKey: admin-user
        passwordKey: admin-password
      grafana.ini:
        server:
          root_url: "https://grafana.keleveni.com"
      persistence:
        enabled: true
        storageClassName: local-path
        size: 2Gi
      service:
        type: ClusterIP
    prometheus:
      prometheusSpec:
        retention: 7d
        securityContext:
          runAsUser: 0
          runAsGroup: 0
          fsGroup: 0
          runAsNonRoot: false
        initContainers:
          - name: fix-permissions
            image: busybox
            command: ["sh", "-c", "chown -R 0:0 /prometheus"]
            volumeMounts:
              - name: prometheus-monitoring-kube-prometheus-prometheus-db
                mountPath: /prometheus
                subPath: prometheus-db
            securityContext:
              runAsUser: 0
              runAsNonRoot: false
        storageSpec:
          volumeClaimTemplate:
            spec:
              storageClassName: local-path
              accessModes: ["ReadWriteOnce"]
              resources:
                requests:
                  storage: 10Gi
    alertmanager:
      enabled: false
    nodeExporter:
      enabled: true
